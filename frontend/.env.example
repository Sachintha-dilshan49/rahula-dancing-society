# Base URL of the backend API (NO trailing slash, NO /api suffix).
# Local development can leave this unset — it falls back to http://localhost:5000.
# In production set it to your deployed backend, e.g. https://api.rahuladance.lk
NEXT_PUBLIC_API_URL=http://localhost:5000
